# Placeholder prompt template for future LLM inference tasks.
# Load with: handler = TemplateHandler.from_yaml("templates/placeholder_template.yaml")
#
# Uncomment and adapt when adding model evaluation to the pipeline.

# messages:
#   - role: system
#     content: "You are a de-identification assistant."
#   - role: user
#     content: |
#       Document: {document}
#       Task: {task}
#   - role: assistant   # SFT target — remove if not doing supervised training
#     content: "<answer>{answer}</answer>"
#
# output_fields:
#   - name: answer
#     pattern: "<answer>(.*?)</answer>"
#     is_list: false
#     required: true
